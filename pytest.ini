[tool:pytest]
markers =
    live_data: marks tests as requiring live data connections (TimescaleDB, Redis)
    slow: marks tests as slow running
    integration: marks tests as integration tests
addopts = 
    --strict-markers
    -v
testpaths = src
python_files = test_*.py
python_classes = Test*
python_functions = test_*